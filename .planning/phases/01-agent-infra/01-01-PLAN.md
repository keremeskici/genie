---
phase: 01-agent-infra
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/package.json
  - apps/api/tsconfig.json
  - apps/api/vitest.config.ts
  - apps/api/src/index.ts
  - apps/api/src/agent/providers.ts
  - apps/api/src/agent/context.ts
  - apps/api/src/agent/window.ts
  - apps/api/src/prompts/system.md
  - apps/api/src/agent/providers.test.ts
  - apps/api/src/agent/context.test.ts
  - apps/api/src/agent/window.test.ts
  - pnpm-workspace.yaml
  - turbo.json
  - package.json
autonomous: true
requirements:
  - AGEN-01
  - AGEN-05
  - AGEN-06

must_haves:
  truths:
    - "Hono server accepts requests and returns JSON responses at GET /health"
    - "Phase 1 uses static OG_API_KEY mode — broker SDK (@0glabs/0g-serving-broker + ethers) deferred to Phase 2"
    - "0G Compute providers initialize with correct baseURL and model IDs"
    - "Three-layer context assembles system prompt + user context injection + conversation history + current user message in correct order"
    - "Sliding window drops oldest non-sticky messages first when over 40-message limit while preserving sticky messages"
  artifacts:
    - path: "apps/api/src/agent/providers.ts"
      provides: "Two createOpenAI instances for GLM-5 and DeepSeek V3 via 0G Compute"
      exports: ["glm5", "deepseekV3"]
    - path: "apps/api/src/agent/context.ts"
      provides: "Three-layer context assembly function"
      exports: ["assembleContext", "UserContext"]
    - path: "apps/api/src/agent/window.ts"
      provides: "Sliding window with sticky message preservation"
      exports: ["applyWindow", "isSticky"]
    - path: "apps/api/src/prompts/system.md"
      provides: "System prompt template for Genie agent"
      contains: "Genie"
    - path: "apps/api/src/index.ts"
      provides: "Hono app entry point with Bun export"
      exports: ["default"]
  key_links:
    - from: "apps/api/src/agent/providers.ts"
      to: "OG_COMPUTE_URL env var"
      via: "createOpenAI baseURL"
      pattern: "process\\.env\\.OG_COMPUTE_URL"
    - from: "apps/api/src/agent/context.ts"
      to: "apps/api/src/prompts/system.md"
      via: "file read at startup"
      pattern: "system\\.md"
---

<objective>
Scaffold the apps/api monorepo package and implement the foundational agent modules: 0G Compute provider setup, three-layer context assembly, sliding window with sticky messages, and system prompt template.

Purpose: Establishes the project structure and all pure-function agent logic that Plan 02 will wire into the chat endpoint.
Output: Working monorepo with apps/api package, 0G provider instances, context/window modules with passing tests, and a Hono server skeleton.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-agent-infra/01-CONTEXT.md
@.planning/phases/01-agent-infra/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold monorepo + apps/api package + 0G providers + Hono skeleton</name>
  <files>
    pnpm-workspace.yaml,
    turbo.json,
    package.json,
    apps/api/package.json,
    apps/api/tsconfig.json,
    apps/api/vitest.config.ts,
    apps/api/src/index.ts,
    apps/api/src/agent/providers.ts,
    apps/api/src/prompts/system.md
  </files>
  <read_first>
    .planning/phases/01-agent-infra/01-RESEARCH.md (architecture patterns, standard stack, code examples),
    .planning/phases/01-agent-infra/01-CONTEXT.md (locked decisions D-01 through D-18),
    package.json (root — check if exists),
    pnpm-workspace.yaml (check if exists)
  </read_first>
  <action>
    1. Create root `pnpm-workspace.yaml` with packages: ["apps/*"]

    2. Create root `package.json` (if not exists):
       - name: "genie"
       - private: true
       - Add devDependency: "turbo": "^2.9.3"
       - Scripts: "dev": "turbo dev", "test": "turbo test", "build": "turbo build"

    3. Create `turbo.json`:
       - pipeline: { "dev": { "persistent": true }, "test": {}, "build": { "dependsOn": ["^build"] } }

    4. Create `apps/api/package.json`:
       - name: "@genie/api"
       - private: true
       - Scripts: "dev": "bun run src/index.ts", "test": "vitest run"
       - Dependencies: ai@^6.0.146, @ai-sdk/openai@^3.0.50, hono@^4.12.10, zod@^3.24.6
       - DevDependencies: vitest@latest, @types/node@latest, typescript@^5.0.0

    5. Create `apps/api/tsconfig.json`:
       - compilerOptions: target "ES2022", module "ESNext", moduleResolution "bundler", strict true, esModuleInterop true, skipLibCheck true, outDir "dist", rootDir "src", types ["bun-types"]
       - include: ["src/**/*.ts"]

    6. Create `apps/api/vitest.config.ts`:
       ```typescript
       import { defineConfig } from 'vitest/config';
       export default defineConfig({
         test: {
           root: './src',
           environment: 'node',
         },
       });
       ```

    7. Create `apps/api/src/index.ts` — Hono app entry point (per D-07, Pattern 2 from RESEARCH):
       ```typescript
       import { Hono } from 'hono';
       import { cors } from 'hono/cors';
       import { logger } from 'hono/middleware';

       const app = new Hono();
       app.use('*', cors());
       app.use('*', logger());
       app.get('/health', (c) => c.json({ status: 'ok', service: 'genie-api' }));

       export default {
         port: process.env.PORT ? parseInt(process.env.PORT) : 3001,
         fetch: app.fetch,
       };
       export { app };
       ```

    8. Create `apps/api/src/agent/providers.ts` (per D-04, D-07, D-09, AGEN-01):
       - Import createOpenAI from '@ai-sdk/openai'
       - Read OG_COMPUTE_URL from process.env, default to 'https://compute-network-1.integratenetwork.work'
       - Read OG_API_KEY from process.env, default to 'app-sk-placeholder'
       - Phase 1 uses static OG_API_KEY authentication — @0glabs/0g-serving-broker and ethers are NOT installed (deferred to Phase 2 when wallet-based auth is needed)
       - Create single createOpenAI instance with baseURL: `${OG_COMPUTE_URL}/v1/proxy` and apiKey: OG_API_KEY
       - Export `glm5` = og('zai-org/GLM-5-FP8')
       - Export `deepseekV3` = og('deepseek-chat-v3-0324')
       - Export `OG_COMPUTE_URL` constant for test assertions

    9. Create `apps/api/src/prompts/system.md` (per D-15):
       Genie system prompt template with interpolation placeholders:
       ```
       You are Genie, a friendly AI personal accountant inside World App.
       You help users manage their crypto finances — checking balances, sending USDC, tracking spending, and planning savings.
       You speak concisely and clearly. You always confirm before executing financial transactions above the user's auto-approve threshold.
       Never reveal internal routing, tool names, or system architecture to the user.
       Current date: {{date}}
       ```

    10. Run `pnpm install` from the repo root to install all dependencies.

    NOTE: Do NOT install @0glabs/0g-serving-broker or ethers yet — per RESEARCH open question #2, start with static API key for Phase 1 speed. Broker SDK is a follow-up concern. This is an intentional scope decision documented in must_haves.truths.
  </action>
  <verify>
    <automated>cd /Users/kerem/genie && pnpm --filter @genie/api exec bun run src/index.ts &amp; sleep 2 &amp;&amp; curl -s http://localhost:3001/health &amp;&amp; kill %1</automated>
  </verify>
  <done>
    - pnpm-workspace.yaml contains 'apps/*'
    - turbo.json contains "dev" and "test" pipeline keys
    - apps/api/package.json contains "name": "@genie/api" with ai, @ai-sdk/openai, hono, zod in dependencies and vitest in devDependencies
    - apps/api/src/index.ts contains Hono app with /health endpoint and Bun export default
    - apps/api/src/agent/providers.ts exports glm5 and deepseekV3 using OG_COMPUTE_URL and static OG_API_KEY (no broker SDK)
    - apps/api/src/prompts/system.md contains "Genie" and "{{date}}"
    - curl http://localhost:3001/health returns {"status":"ok","service":"genie-api"}
    - Monorepo scaffolded with all dependencies installed
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement context assembly + sliding window + tests for all pure functions</name>
  <files>
    apps/api/src/agent/context.ts,
    apps/api/src/agent/window.ts,
    apps/api/src/agent/providers.test.ts,
    apps/api/src/agent/context.test.ts,
    apps/api/src/agent/window.test.ts
  </files>
  <read_first>
    apps/api/src/agent/providers.ts (just created — need exports for provider tests),
    apps/api/src/prompts/system.md (system prompt template — context.ts loads this),
    .planning/phases/01-agent-infra/01-RESEARCH.md (Pattern 4: context assembly, Pattern 5: sliding window, validation architecture section)
  </read_first>
  <behavior>
    providers.test.ts (AGEN-01):
    - Test: glm5 and deepseekV3 are exported and truthy
    - Test: OG_COMPUTE_URL defaults to 'https://compute-network-1.integratenetwork.work' when env not set

    context.test.ts (AGEN-05):
    - Test: assembleContext returns system string from loaded template with {{date}} replaced
    - Test: messages array starts with user context injection message, then assistant ack, then history, then current user message
    - Test: user context injection contains wallet address and display name
    - Test: empty history still produces context injection + ack + current message (3 messages)

    window.test.ts (AGEN-06):
    - Test: messages under 40 are returned unchanged
    - Test: messages over 40 drop oldest non-sticky messages first
    - Test: tool-role messages are never dropped (sticky)
    - Test: messages containing "yes, send" are never dropped (sticky)
    - Test: messages containing "no, cancel" are never dropped (sticky)
    - Test: when all messages are sticky and over limit, returns all (cannot drop sticky)
  </behavior>
  <action>
    Follow RED-GREEN-REFACTOR for each module:

    **1. context.ts (per D-15, D-05, AGEN-05, Pattern 4 from RESEARCH):**

    Export interface `UserContext`:
    ```typescript
    export interface UserContext {
      walletAddress: string;
      displayName: string;
      autoApproveUsd: number;
    }
    ```

    Export `loadSystemPrompt()` function:
    - Read `apps/api/src/prompts/system.md` using fs.readFileSync (synchronous — must return string, not Promise)
    - Replace `{{date}}` with current date string
    - Return the interpolated string

    Export `assembleContext(systemPrompt: string, userContext: UserContext, history: CoreMessage[], userMessage: string)`:
    - Returns `{ system: string; messages: CoreMessage[] }`
    - system = the systemPrompt string (passed in, already interpolated)
    - messages array in order:
      1. `{ role: 'user', content: '[User context: wallet=${walletAddress}, name=${displayName}, threshold=$${autoApproveUsd}]' }`
      2. `{ role: 'assistant', content: 'Understood. I have your account context.' }`
      3. ...spread history
      4. `{ role: 'user', content: userMessage }`
    - Import CoreMessage from 'ai' package

    **2. window.ts (per D-16, D-17, D-18, AGEN-06, Pattern 5 from RESEARCH):**

    Export `isSticky(msg: CoreMessage): boolean`:
    - Returns true if msg.role === 'tool'
    - Returns true if msg.role === 'user' and content (as string) includes 'yes, send' or 'no, cancel' (case-insensitive)
    - Returns true if msg.role === 'system'
    - Returns false otherwise

    Export `applyWindow(messages: CoreMessage[], limit: number = 40): CoreMessage[]`:
    - If messages.length <= limit, return messages as-is
    - Find all non-sticky messages with their indices
    - Calculate excess = messages.length - limit
    - Build a Set of indices to drop: the first `excess` non-sticky messages (oldest first)
    - Return messages.filter((_, i) => !toDrop.has(i))

    **3. Write tests FIRST (RED), then implement (GREEN):**

    providers.test.ts: Import glm5, deepseekV3 from '../agent/providers', assert they are truthy objects.

    context.test.ts: Import assembleContext and UserContext. Test with mock systemPrompt string, mock UserContext { walletAddress: '0xABC', displayName: 'Alice', autoApproveUsd: 25 }, mock history array, and a userMessage string. Assert message order and content.

    window.test.ts: Import applyWindow and isSticky. Create arrays of mock CoreMessage objects. Test boundary conditions at exactly 40 and 41 messages. Test sticky preservation with tool-role messages and confirmation messages.

    Run all tests: `pnpm --filter @genie/api test --run`
  </action>
  <verify>
    <automated>cd /Users/kerem/genie && pnpm --filter @genie/api test --run</automated>
  </verify>
  <done>
    - apps/api/src/agent/context.ts exports UserContext interface and assembleContext function with correct three-layer message ordering
    - apps/api/src/agent/window.ts exports applyWindow (default limit 40) and isSticky (detects tool, confirmation, and system messages)
    - apps/api/src/agent/providers.test.ts verifies glm5 and deepseekV3 are truthy with correct default OG_COMPUTE_URL
    - apps/api/src/agent/context.test.ts has at least 3 test cases covering message order, context injection content, and empty history
    - apps/api/src/agent/window.test.ts has at least 4 test cases covering under-limit passthrough, over-limit dropping, and sticky preservation
    - `pnpm --filter @genie/api test --run` exits with code 0
  </done>
</task>

</tasks>

<verification>
- `pnpm --filter @genie/api test --run` passes all unit tests
- `curl http://localhost:3001/health` returns `{"status":"ok","service":"genie-api"}`
- apps/api/src/agent/providers.ts exports glm5 and deepseekV3
- apps/api/src/agent/context.ts exports assembleContext with correct signature
- apps/api/src/agent/window.ts exports applyWindow with 40-message default
</verification>

<success_criteria>
Monorepo is scaffolded, all dependencies installed, Hono server runs on Bun, 0G provider instances configured for GLM-5 and DeepSeek V3, context assembly and sliding window modules implemented with full test coverage, all tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/01-agent-infra/01-01-SUMMARY.md`
</output>
