---
phase: 08-identity-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/routes/verify.ts
  - apps/api/src/routes/verify.test.ts
  - apps/api/vitest.config.ts
  - apps/web/src/app/api/verify-proof/route.ts
autonomous: true
requirements: [WRID-02, WRID-04]
must_haves:
  truths:
    - "Backend verify endpoint accepts slim payload (userId + nullifier_hash only) and stores worldId"
    - "BFF verify-proof sends only userId and nullifier_hash to backend after World ID Cloud API validation"
    - "Backend does NOT call World ID Cloud API (single validation in BFF only)"
    - "All verify.test.ts tests pass with updated schema expectations"
  artifacts:
    - path: "apps/api/src/routes/verify.ts"
      provides: "Simplified verify endpoint trusting BFF"
      contains: "z.object"
    - path: "apps/web/src/app/api/verify-proof/route.ts"
      provides: "BFF with slim payload to backend"
      contains: "nullifier_hash"
    - path: "apps/api/src/routes/verify.test.ts"
      provides: "Updated tests for simplified schema"
      contains: "mockFetch"
    - path: "apps/api/vitest.config.ts"
      provides: "Missing env vars for test suites"
      contains: "WORLD_APP_ID"
  key_links:
    - from: "apps/web/src/app/api/verify-proof/route.ts"
      to: "apps/api/src/routes/verify.ts"
      via: "POST /api/verify with {userId, nullifier_hash}"
      pattern: "nullifier_hash.*userId"
---

<objective>
Simplify the verify flow to single-validation architecture: BFF owns World ID Cloud API validation, backend trusts BFF and only stores the nullifier_hash. Also fix vitest env gaps.

Purpose: Eliminates double-validation bug (audit #3) per D-02/D-03. Backend verify endpoint accepts only userId + nullifier_hash from trusted BFF.
Output: Simplified verify.ts, updated verify-proof BFF route, updated tests, fixed vitest config.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-identity-wiring/08-CONTEXT.md
@.planning/phases/08-identity-wiring/08-RESEARCH.md

@apps/api/src/routes/verify.ts
@apps/api/src/routes/verify.test.ts
@apps/api/vitest.config.ts
@apps/web/src/app/api/verify-proof/route.ts
@apps/api/src/routes/chat.ts

<interfaces>
<!-- Key types and contracts the executor needs -->

From apps/api/src/routes/chat.ts:
```typescript
export function invalidateContextCache(userId: string): void;
export async function resolveUserId(rawUserId: string): Promise<string | null>;
```

From apps/api/src/config/env.ts:
```typescript
export const WORLD_APP_ID: string;
export const WORLD_ACTION: string;
export const WORLD_VERIFY_API_URL: string;
```

From apps/web/src/auth/index.ts:
```typescript
export const { handlers, signIn, signOut, auth } = NextAuth({...});
// session.user.id = UUID (from backend provisioning)
// session.user.walletAddress = 0x address
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Simplify backend verify.ts + update BFF verify-proof payload</name>
  <files>apps/api/src/routes/verify.ts, apps/web/src/app/api/verify-proof/route.ts</files>
  <read_first>
    - apps/api/src/routes/verify.ts (current proofSchema with 5 fields, World ID Cloud API call)
    - apps/web/src/app/api/verify-proof/route.ts (current BFF sending full proof payload)
    - apps/api/src/routes/chat.ts (invalidateContextCache and resolveUserId exports)
    - apps/api/src/config/env.ts (WORLD_APP_ID, WORLD_ACTION, WORLD_VERIFY_API_URL exports)
  </read_first>
  <action>
**Backend verify.ts (per D-02/D-03):**

1. Replace the `proofSchema` with a slim schema:
```typescript
const proofSchema = z.object({
  userId: z.string().min(1),
  nullifier_hash: z.string().min(1),
});
```

2. Remove the `proof`, `merkle_root`, `verification_level` destructuring from `parsed.data`. Only destructure `userId: rawUserId` and `nullifier_hash`.

3. Delete the entire World ID Cloud API fetch block (lines ~42-51 in current file: the `fetch(WORLD_VERIFY_API_URL/...)` call and its error handling `if (!worldResponse.ok)` block).

4. Remove the imports that are no longer needed: `WORLD_APP_ID`, `WORLD_ACTION`, `WORLD_VERIFY_API_URL` from `'../config/env'`. The import line should be deleted entirely since no env vars are used anymore.

5. Keep everything else unchanged: `resolveUserId`, the user existence check, the `ALREADY_VERIFIED` check, the `db.update` to store `nullifier_hash` in `users.worldId`, and `invalidateContextCache(userId)`.

The resulting route handler body should be approximately:
- Parse slim body
- resolveUserId
- Check user exists + not already verified
- Store nullifier_hash to users.worldId
- Invalidate context cache
- Return `{ success: true }`

**BFF verify-proof route.ts (per D-02):**

1. In the success block (after `if (verifyRes.success)`), change the `fetch` call to the backend to send ONLY `userId` and `nullifier_hash`:
```typescript
await fetch(`${apiUrl}/api/verify`, {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    userId,
    nullifier_hash: (payload as Record<string, unknown>).nullifier_hash,
  }),
});
```

2. Remove the lines that send `proof`, `merkle_root`, and `verification_level` in the JSON body.

3. Keep the rest of the BFF route unchanged: World ID Cloud API validation call, session fetching via `auth()`, error handling, response format.
  </action>
  <verify>
    <automated>cd /Users/kerem/genie/apps/api && npx vitest run routes/verify.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - apps/api/src/routes/verify.ts contains `z.object({` with exactly two fields: `userId: z.string().min(1)` and `nullifier_hash: z.string().min(1)`
    - apps/api/src/routes/verify.ts does NOT contain the string `WORLD_VERIFY_API_URL`
    - apps/api/src/routes/verify.ts does NOT contain the string `WORLD_APP_ID`
    - apps/api/src/routes/verify.ts does NOT contain `fetch(` (no outbound HTTP call)
    - apps/api/src/routes/verify.ts still contains `invalidateContextCache(userId)`
    - apps/api/src/routes/verify.ts still contains `db.update(users).set({ worldId: nullifier_hash })`
    - apps/web/src/app/api/verify-proof/route.ts backend fetch body contains `nullifier_hash` and `userId` but NOT `proof` or `merkle_root` or `verification_level`
    - apps/web/src/app/api/verify-proof/route.ts still contains the World ID Cloud API fetch (`developer.worldcoin.org` or `WORLD_VERIFY_API_URL`)
  </acceptance_criteria>
  <done>Backend verify accepts only userId + nullifier_hash from trusted BFF. BFF sends slim payload. No double validation.</done>
</task>

<task type="auto">
  <name>Task 2: Update verify.test.ts + fix vitest env config</name>
  <files>apps/api/src/routes/verify.test.ts, apps/api/vitest.config.ts</files>
  <read_first>
    - apps/api/src/routes/verify.test.ts (current tests with mockFetch for World ID portal)
    - apps/api/vitest.config.ts (current env section missing WORLD_APP_ID, WORLD_ACTION)
    - apps/api/src/routes/verify.ts (the file just modified in Task 1 -- read to confirm new schema)
  </read_first>
  <action>
**verify.test.ts updates:**

1. Change `validBody` to only include the slim fields:
```typescript
const validBody = {
  userId: '00000000-0000-0000-0000-000000000001',
  nullifier_hash: '0xnull123',
};
```

2. Remove the `mockFetch` setup and `global.fetch = mockFetch` line. The backend no longer makes any fetch calls, so mocking global fetch is unnecessary.

3. Update test "returns 400 INVALID_INPUT when proof field missing" — change the body to `{ userId: '00000000-0000-0000-0000-000000000001' }` (missing `nullifier_hash`). This test should still get 400 because the slim schema requires `nullifier_hash`.

4. Delete the test "returns 400 VERIFICATION_FAILED when portal rejects proof" — this test is no longer relevant because the backend does not call the World ID portal.

5. Delete the test "calls portal at correct URL with action merged" — same reason, no portal call.

6. Update the test "returns success, stores nullifier, invalidates cache on valid proof":
   - Remove `mockFetch.mockResolvedValue(...)` setup
   - Keep `mockSelect.mockResolvedValue([{ worldId: null }])` (user exists, not verified)
   - Keep assertions: `mockSet` called with `{ worldId: '0xnull123' }` and `mockInvalidate` called with userId
   - Add assertion: `expect(global.fetch).not.toHaveBeenCalled()` — OR simply remove any reference to mockFetch since it's deleted

7. Remove the `vi.mock('../config/env', ...)` block since the backend verify.ts no longer imports env config.

**vitest.config.ts updates:**

Add the following env vars to the `env` section (fixes 4 pre-existing failing test suites):
```typescript
WORLD_APP_ID: 'app_test123',
WORLD_ACTION: 'verify-human',
WORLD_VERIFY_API_URL: 'https://developer.world.org/api/v2/verify',
```
  </action>
  <verify>
    <automated>cd /Users/kerem/genie/apps/api && npx vitest run routes/verify.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - apps/api/src/routes/verify.test.ts does NOT contain the string `mockFetch`
    - apps/api/src/routes/verify.test.ts does NOT contain `global.fetch`
    - apps/api/src/routes/verify.test.ts `validBody` contains only `userId` and `nullifier_hash` (no `proof`, `merkle_root`, `verification_level`)
    - apps/api/src/routes/verify.test.ts does NOT contain test description "calls portal at correct URL"
    - apps/api/src/routes/verify.test.ts does NOT contain test description "VERIFICATION_FAILED when portal rejects"
    - apps/api/src/routes/verify.test.ts does NOT contain `vi.mock('../config/env'`
    - apps/api/vitest.config.ts contains `WORLD_APP_ID: 'app_test123'`
    - apps/api/vitest.config.ts contains `WORLD_ACTION: 'verify-human'`
    - `cd apps/api && npx vitest run routes/verify.test.ts` exits 0 with all tests passing
  </acceptance_criteria>
  <done>All verify tests pass against simplified schema. No portal mock references remain. Vitest env vars added for cross-suite stability.</done>
</task>

</tasks>

<verification>
- `cd apps/api && npx vitest run routes/verify.test.ts` — all tests pass
- `cd apps/api && npx vitest run` — full suite runs (previously-failing suites now have env vars)
- `grep -c "fetch(" apps/api/src/routes/verify.ts` returns 0 (no outbound fetch)
- `grep "nullifier_hash" apps/web/src/app/api/verify-proof/route.ts` — slim payload confirmed
</verification>

<success_criteria>
- Backend verify.ts accepts only {userId, nullifier_hash} — no proof, merkle_root, verification_level
- Backend verify.ts makes zero outbound HTTP calls (no World ID Cloud API)
- BFF verify-proof sends slim payload to backend
- All verify.test.ts tests pass with no portal mock
- vitest.config.ts has WORLD_APP_ID and WORLD_ACTION env vars
</success_criteria>

<output>
After completion, create `.planning/phases/08-identity-wiring/08-01-SUMMARY.md`
</output>
