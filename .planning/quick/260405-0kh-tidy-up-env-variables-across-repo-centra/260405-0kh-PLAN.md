---
phase: quick
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/config/env.ts
  - apps/api/src/chain/clients.ts
  - apps/api/src/tools/resolve-contact.ts
  - apps/api/src/routes/verify.ts
  - apps/web/src/components/Transaction/index.tsx
  - apps/web/src/app/api/verify-proof/route.ts
  - apps/web/src/app/api/rp-signature/route.ts
  - .env
  - .env.example
autonomous: true
requirements: []
must_haves:
  truths:
    - "Zero hardcoded URLs, contract addresses, or API endpoints in source code"
    - "All configurable values read from process.env via centralized config"
    - ".env and .env.example contain every variable the app needs"
  artifacts:
    - path: "apps/api/src/config/env.ts"
      provides: "Centralized env config for API server"
    - path: ".env"
      provides: "All env variables in one place"
    - path: ".env.example"
      provides: "Template with every variable documented"
  key_links:
    - from: "apps/api/src/chain/clients.ts"
      to: "apps/api/src/config/env.ts"
      via: "import"
      pattern: "from.*config/env"
    - from: "apps/api/src/tools/resolve-contact.ts"
      to: "apps/api/src/config/env.ts"
      via: "import"
      pattern: "from.*config/env"
---

<objective>
Centralize all environment variables, remove every hardcoded URL, RPC endpoint, contract address, and API key from source code. Create a single config module that all API code imports from, and ensure .env/.env.example are the single source of truth.

Purpose: Make the app fully configurable via .env without touching source code.
Output: Centralized env config, updated source files, complete .env and .env.example.
</objective>

<context>
@.env
@.env.example
@apps/api/src/chain/clients.ts
@apps/api/src/tools/resolve-contact.ts
@apps/api/src/routes/verify.ts
@apps/api/src/agent/providers.ts
@apps/web/src/app/api/verify-proof/route.ts
@apps/web/src/app/api/rp-signature/route.ts
@apps/web/src/components/Transaction/index.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create centralized env config and add new env vars to .env/.env.example</name>
  <files>apps/api/src/config/env.ts, .env, .env.example</files>
  <action>
1. Create `apps/api/src/config/env.ts` that centralizes ALL API-side env reads into typed, validated exports. Use a simple requireEnv helper (like the one already in providers.ts). Group by concern:

```
// --- 0G Compute ---
export const OG_COMPUTE_URL = requireEnv('OG_COMPUTE_URL');
export const OG_API_KEY = requireEnv('OG_API_KEY');
export const OG_PLANNING_MODEL = requireEnv('OG_PLANNING_MODEL');
export const OG_ACTION_MODEL = requireEnv('OG_ACTION_MODEL');

// --- 0G KV Storage ---
export const OG_KV_CLIENT_URL = optionalEnv('OG_KV_CLIENT_URL');
export const OG_PRIVATE_KEY = optionalEnv('OG_PRIVATE_KEY');
export const OG_KV_STREAM_ID = optionalEnv('OG_KV_STREAM_ID');

// --- World ID ---
export const WORLD_APP_ID = requireEnv('WORLD_APP_ID');
export const WORLD_ACTION = requireEnv('WORLD_ACTION');
export const WORLD_VERIFY_API_URL = optionalEnv('WORLD_VERIFY_API_URL') ?? 'https://developer.world.org/api/v2/verify';
export const WORLD_USERNAME_API_URL = optionalEnv('WORLD_USERNAME_API_URL') ?? 'https://usernames.worldcoin.org/api/v1';

// --- World Chain ---
export const WORLD_CHAIN_RPC_URL = optionalEnv('WORLD_CHAIN_RPC_URL');
export const WORLD_CHAIN_TESTNET = process.env.WORLD_CHAIN_TESTNET === 'true';
export const RELAYER_PRIVATE_KEY = optionalEnv('RELAYER_PRIVATE_KEY');
export const GENIE_ROUTER_ADDRESS = (optionalEnv('GENIE_ROUTER_ADDRESS') ?? '0x0000000000000000000000000000000000000000') as `0x${string}`;
export const PAY_HANDLER_ADDRESS = (optionalEnv('PAY_HANDLER_ADDRESS') ?? '0x0000000000000000000000000000000000000000') as `0x${string}`;
export const USDC_ADDRESS_TESTNET = optionalEnv('USDC_ADDRESS_TESTNET') ?? '0x66145f38cBAC35Ca6F1Dfb4914dF98F1614aeA88';
export const USDC_ADDRESS_MAINNET = optionalEnv('USDC_ADDRESS_MAINNET') ?? '0x79A02482A880bCE3F13e09Da970dC34db4CD24d1';

// --- API Server ---
export const PORT = parseInt(process.env.PORT ?? '3001', 10);
export const MAX_OUTPUT_TOKENS = parseInt(process.env.MAX_OUTPUT_TOKENS ?? '2048', 10);
export const WINDOW_LIMIT = parseInt(process.env.WINDOW_LIMIT ?? '40', 10);
```

The `requireEnv` helper throws with a clear message. The `optionalEnv` helper returns `string | undefined`.

2. Add these NEW env vars to both `.env` and `.env.example` (append to the appropriate sections):
   - `WORLD_VERIFY_API_URL=https://developer.world.org/api/v2/verify`
   - `WORLD_USERNAME_API_URL=https://usernames.worldcoin.org/api/v1`
   - `USDC_ADDRESS_TESTNET=0x66145f38cBAC35Ca6F1Dfb4914dF98F1614aeA88`
   - `USDC_ADDRESS_MAINNET=0x79A02482A880bCE3F13e09Da970dC34db4CD24d1`

Keep existing values in .env intact. In .env.example, use the default URLs (not secrets).
  </action>
  <verify>
    <automated>cd /Users/kerem/genie && npx tsx apps/api/src/config/env.ts 2>&1 | head -5; grep -c "WORLD_VERIFY_API_URL\|WORLD_USERNAME_API_URL\|USDC_ADDRESS_TESTNET\|USDC_ADDRESS_MAINNET" .env</automated>
  </verify>
  <done>Centralized env config file exists with all variables typed and grouped. .env and .env.example contain every variable including the new ones (WORLD_VERIFY_API_URL, WORLD_USERNAME_API_URL, USDC_ADDRESS_TESTNET, USDC_ADDRESS_MAINNET).</done>
</task>

<task type="auto">
  <name>Task 2: Refactor all source files to import from centralized config</name>
  <files>apps/api/src/agent/providers.ts, apps/api/src/agent/index.ts, apps/api/src/chain/clients.ts, apps/api/src/tools/resolve-contact.ts, apps/api/src/routes/verify.ts, apps/api/src/kv/client.ts, apps/api/src/kv/memory.ts, apps/api/src/index.ts, apps/web/src/components/Transaction/index.tsx, apps/web/src/app/api/verify-proof/route.ts, apps/web/src/app/api/rp-signature/route.ts</files>
  <action>
Refactor each file to remove inline `process.env` reads and hardcoded values. Import from `@/config/env` (or relative `../config/env`) instead.

**apps/api/src/agent/providers.ts:**
- Remove the local `requireEnv` helper and the 4 env reads at top.
- Import `{ OG_COMPUTE_URL, OG_API_KEY, PLANNING_MODEL, ACTION_MODEL }` from `../config/env`.
- Keep the rest of the file (createOpenAI, og.chat calls) unchanged.

**apps/api/src/agent/index.ts:**
- Remove `parseInt(process.env.MAX_OUTPUT_TOKENS ?? '2048', 10)` and `parseInt(process.env.WINDOW_LIMIT ?? '40', 10)`.
- Import `{ MAX_OUTPUT_TOKENS, WINDOW_LIMIT }` from `../config/env`.

**apps/api/src/index.ts:**
- Remove `parseInt(process.env.PORT ?? '3001', 10)`.
- Import `{ PORT }` from `./config/env`.

**apps/api/src/chain/clients.ts:**
- Remove all `process.env` reads.
- Import `{ WORLD_CHAIN_RPC_URL, WORLD_CHAIN_TESTNET, RELAYER_PRIVATE_KEY, GENIE_ROUTER_ADDRESS, PAY_HANDLER_ADDRESS, USDC_ADDRESS_TESTNET, USDC_ADDRESS_MAINNET }` from `../config/env`.
- Replace hardcoded USDC ternary with: `export const USDC_ADDRESS: \`0x\${string}\` = (WORLD_CHAIN_TESTNET ? USDC_ADDRESS_TESTNET : USDC_ADDRESS_MAINNET) as \`0x\${string}\`;`
- Use imported `WORLD_CHAIN_TESTNET` for chain selection.
- Use imported `RELAYER_PRIVATE_KEY` in getRelayerAccountInstance.

**apps/api/src/tools/resolve-contact.ts:**
- Remove hardcoded `const WORLD_USERNAME_API = 'https://usernames.worldcoin.org/api/v1';`
- Import `{ WORLD_USERNAME_API_URL }` from `../config/env` and use it.

**apps/api/src/routes/verify.ts:**
- Remove `process.env.WORLD_APP_ID!` and `process.env.WORLD_ACTION!`.
- Import `{ WORLD_APP_ID, WORLD_ACTION, WORLD_VERIFY_API_URL }` from `../config/env`.
- Replace hardcoded `https://developer.world.org/api/v2/verify/${appId}` with `${WORLD_VERIFY_API_URL}/${WORLD_APP_ID}`.

**apps/api/src/kv/client.ts:**
- Remove direct `process.env.OG_KV_CLIENT_URL`, `process.env.OG_PRIVATE_KEY`, `process.env.OG_KV_STREAM_ID`.
- Import `{ OG_KV_CLIENT_URL, OG_PRIVATE_KEY, OG_KV_STREAM_ID }` from `../config/env`.

**apps/api/src/kv/memory.ts:**
- Remove `process.env.OG_KV_STREAM_ID`.
- Import `{ OG_KV_STREAM_ID }` from `../config/env`.

**apps/web/src/components/Transaction/index.tsx:**
- This is a World MiniKit template component. Replace hardcoded contract address `0xF0882554ee924278806d708396F1a7975b732522` with `process.env.NEXT_PUBLIC_TEST_CONTRACT_ADDRESS!` and hardcoded RPC URL `https://worldchain-mainnet.g.alchemy.com/public` with `process.env.NEXT_PUBLIC_WORLD_CHAIN_RPC_URL!`. Add both to .env and .env.example under a Web section.
- Note: Web/Next.js files must use `process.env.NEXT_PUBLIC_*` directly (Next.js inlines at build time), so no centralized config for web -- just remove hardcoded values.

**apps/web/src/app/api/verify-proof/route.ts:**
- The verify URL `https://developer.worldcoin.org/api/v2/verify/${app_id}` is hardcoded. Replace with `${process.env.WORLD_VERIFY_API_URL ?? 'https://developer.worldcoin.org/api/v2/verify'}/${app_id}`. Add `WORLD_VERIFY_API_URL` to .env.example under Web section (already added to .env in Task 1).

**apps/web/src/app/api/rp-signature/route.ts:**
- The RP_ID fallback `'rp_e87d44dbb7b76d91'` is hardcoded. This is fine since it's already reading from `process.env.RP_ID` with a fallback. No change needed -- the fallback is just a default format, and the real value is in .env.

After all changes, ensure no `process.env` reads remain in `apps/api/src/` except inside `config/env.ts` and test files (`*.test.ts`, `vitest.config.ts`).
  </action>
  <verify>
    <automated>cd /Users/kerem/genie && grep -rn "process\.env\." apps/api/src/ --include="*.ts" | grep -v "config/env.ts" | grep -v ".test.ts" | grep -v "vitest.config" | head -20; echo "---"; npx tsc --noEmit -p apps/api/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>
- Zero `process.env` reads in apps/api/src/ outside of config/env.ts and test files.
- All hardcoded URLs (World verify API, World username API, Alchemy RPC) replaced with env var references.
- All hardcoded contract addresses (USDC testnet/mainnet) replaced with env var references.
- TypeScript compiles without errors.
- .env and .env.example are complete with every variable the app references.
  </done>
</task>

</tasks>

<verification>
1. `grep -rn "process\.env\." apps/api/src/ --include="*.ts" | grep -v config/env | grep -v test | grep -v vitest` returns empty
2. `grep -rn "hardcoded\|localhost" apps/api/src/ --include="*.ts" | grep -v config/env | grep -v test | grep -v vitest` returns empty (except comments)
3. `grep -c "=" .env.example` shows all variables documented
4. TypeScript compiles: `npx tsc --noEmit -p apps/api/tsconfig.json`
</verification>

<success_criteria>
- Every configurable value in apps/api/src/ reads from the centralized config/env.ts module
- No hardcoded URLs, RPC endpoints, contract addresses, or API keys in source files
- .env contains every variable with current values
- .env.example contains every variable with safe placeholder values
- All existing tests still pass (env vars set in vitest.config.ts)
</success_criteria>
